import reverseWords from '../';

describe('reverseWords', () => {
  it('should behave...', () => {
    expect(reverseWords('I like JavaScript problems')).toEqual(
      'I ekil tpircSavaJ smelborp'
    );
  });
});
